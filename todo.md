# vectordb — Rust 製インプロセス・ベクトルデータベース 実装計画

軽量・高速で、アプリケーションに直接組み込めるベクトルデータベースを Rust で実装する。
サーバー不要・設定不要のローカルライブラリとして、密/疎ベクトル検索・全文検索・スカラー
フィルタ・ハイブリッド検索を単一のクエリ API で提供する。

このファイルは実装の唯一のロードマップである。各マイルストーンは「完了条件」を満たした
時点でチェックを付け、順番に進める(先行マイルストーンの完了条件が後続の前提になる)。
覆すコストが高い決定は必ず ADR(`docs/adr/`)に記録する。

---

## 0. 設計原則

- **インプロセス・組み込み**: `Collection::open(path)` だけで動く。デーモン・設定ファイル不要。
- **明確なコミット意味論**: 「書き込み成功」と「耐久コミット」を区別する。既定では書き込みは
  WAL に追記された時点で成功を返し(fsync はしない)、`flush()` 完了時点のみを耐久コミット
  点とする。オプションで同期 fsync モードを提供。この契約を rustdoc に明記する。
- **クラッシュ安全**: 単一の権威 WAL + 世代付きマニフェスト + アトミック rename。プロセス
  kill 後も開き直せば「最後の耐久コミット点 + WAL から回復できた分」に確定的に戻る。
  fsync の失敗は握りつぶさない: 発生時はコレクションを poisoned(以後 read-only)状態に
  遷移させ、成功を偽らない。
- **単一ライタ・複数リーダ**: 書き込みは 1 プロセスに限定(排他ファイルロック)。読み取り
  専用オープンはロックを取らず、マニフェスト世代のスナップショット分離で一貫性を得る。
  ローカルファイルシステム前提(NFS 等は非サポートと明記)。
- **一貫した読み取りビュー(単一公開エポック)**: ライタは flush 完了時に
  {マニフェスト世代, 削除スナップショット, KV スナップショット, 可視 LSN 上限} を
  1 個のエポックオブジェクトとして**アトミックに公開**(`arc-swap`)し、プロセス内
  リーダはそれを取得して使う(リーダが自分でバラバラに取得しない)。ファイルは `Arc`
  参照。flush / compaction はピン中のビューが解放されるまで、対象の物理削除
  (ファイル・KV キー範囲とも)を遅延する。別プロセスの read-only リーダの一貫性
  規則は M3 に定義する。
- **log-ahead-of-data 規則**: KV(RocksDB)の memtable がディスクへ flush される前に、
  その内容が依存する最大 LSN まで自前 WAL を fsync する(flush イベントフックで強制)。
  これにより「KV に永続化されたが WAL に存在しない操作」を構造的に排除する。
- **セグメント指向**: 書き込み中セグメント(可変)+ 確定済みセグメント(不変)。削除は
  論理削除(bitmap)、compaction で物理回収。
- **unsafe 最小化**: unsafe は (a) SIMD カーネル、(b) mmap 境界、(c) グラフ隣接リストの
  atomic 公開プロトコル、の 3 箇所に閉じ込める。(a) はスカラー実装との等価性テスト、
  (c) は loom モデル + ThreadSanitizer を必須とする。
- **フォーマットはバージョン付き**: 自前バイナリファイルは magic + version ヘッダ +
  CRC32C を持ち、未知バージョンは明示的なエラーで拒否する。JSON マニフェストは
  `format_version` フィールド、KV ストアは open 時に検証する schema-version キーで管理
  (サードパーティ標準形式のファイルに私製ヘッダは付けない)。
- **公開 API に依存クレートの型を出さない**: エラーは自前 `Error` に変換し、rocksdb /
  arrow 等の型・エラーが public シグネチャに現れないこと(semver 独立性)。

## 1. 全体アーキテクチャ

```
crates/
  vectordb/         公開 API(Collection, Schema, Doc, Query, エラー型)
  vectordb-core/    共通型(DataType, Metric, Status/Error, 設定, 制限値)
  vectordb-simd/    距離カーネル(スカラー参照実装 + ランタイム SIMD ディスパッチ)
  vectordb-index/   ベクトルインデックス(Flat / HNSW / IVF / グラフ)+ 量子化
  vectordb-store/   セグメント, WAL, マニフェスト, forward store, 削除ビットマップ, PK マップ
  vectordb-scalar/  スカラー転置インデックス + フィルタ式パーサ/実行器
  vectordb-text/    全文検索(トークナイザ, 転置, BM25)
```

主要依存クレート(方針): `rocksdb`(可変状態の KV。**コレクションにつき 1 インスタンス**、
RocksDB 自身の WAL は無効化・`atomic_flush` 有効化。耐久性は自前 WAL に一本化)、
`arrow`(forward store)、`roaring`(doc-id は u64 なので **`RoaringTreemap`** を使用。
u32 の `RoaringBitmap` との取り違えに注意し、`u32::MAX` 境界のテストを置く)、`rayon`、
`parking_lot`、`arc-swap` / `crossbeam-epoch`(グラフ隣接リストの COW 公開と回収)、
`memmap2`、`crc32c`(Castagnoli。IEEE CRC32 の `crc32fast` とは非互換 — 既知ベクタ
テストを全フォーマットモジュールに置く)、`half`、`serde`(マニフェスト)、`fd-lock`、
`thiserror`。テキスト系: `unicode-normalization`、`unicode-segmentation`、
`rust-stemmers`、CJK 辞書トークナイザ(`jieba-rs` 等)、`bitpacking`。

### データモデル

- フィールド型: `Bool / I32 / I64 / U32 / U64 / F32 / F64 / String / Binary` +
  各スカラーの配列型 + 密ベクトル(`F32 / F16 / I8`)+ 疎ベクトル(`F32 / F16`)。
- 主キーは文字列(スキーマ外の暗黙フィールド)。内部では単調増加の **`DocId`(u64 の
  newtype、グローバル一意)** に写像。doc-id は再利用せず、次値カウンタは常に
  「コミット済みマニフェストの値 + リプレイで観測した最大値 + 1」から再開する
  (ディスク走査からは導出しない)。
- 浮動小数値(スカラー・配列・ベクトルすべて)は有限値のみ受理(NaN / ±inf は Doc 検証で
  InvalidArgument。距離とフィルタの順序を決定的に保つため)。「値なし」は nullable + null で
  表現する。将来 ±inf の実需が出た場合、受理範囲を広げる変更は非破壊で行える。
- nullable フィールド対応。ベクトルは次元固定・完全一致を検証。
- 制限値(`vectordb-core` に一元定義、原則コンパイル時定数ではなく設定可能な既定値):
  最大次元 16,384 / 疎ベクトル最大格納エントリ数 65,536(零値も格納・カウントする)/
  コレクション最大ベクトル
  フィールド数 4 / topk 上限 65,536 / バッチ書き込み上限 10,000 など。

#### CRUD 意味論(確定仕様 — M3 で実装しテストする)

| 操作 | PK 前提 | 意味 | 欠落フィールド | 明示 null |
|---|---|---|---|---|
| insert | 存在しない(存在時 AlreadyExists) | 全置換の新規作成 | non-nullable → InvalidArgument / nullable → null | nullable のみ可 |
| upsert | どちらでも可 | 全置換(旧 doc-id を論理削除し新 doc-id で挿入) | insert と同じ | insert と同じ |
| update | 存在する(不在時 NotFound) | **パッチ**: 旧文書を取得しマージ後、旧 doc-id 論理削除 + 新 doc-id 挿入 | 「変更しない」の意 | null に上書き(nullable のみ) |
| delete | 不在/削除済みは NotFound を返す | 論理削除(bitmap)+ PK マップから除去 | — | — |

- バッチ書き込みは文書毎に独立の Status を返す(バッチ全体のアトミック性なし)。
- **WAL には実行時に解決済みの完全な状態を記録する**: update はマージ後の全文書を、
  全操作は旧/新 doc-id を明示して記録。リプレイが過去のセグメントを再読しない。
- **リプレイ遷移表**(公開 API の前提条件とは独立に定義): 各操作について「KV / 削除
  bitmap が既に部分適用済みの状態」からの遷移を定義し、predicated な公開 API 検証は
  適用しない(例: delete リプレイは PK 不在でも成功扱い、upsert リプレイは記録された
  旧 doc-id のみを削除)。記録と矛盾する予期しないマッピングは Corruption。KV への
  通常パス書き込みは blind put のみとし、読み‐変更‐書きで蓄積する構造
  (位置リストの追記等)を KV に置かない(蓄積はセグメント確定時の一括構築で行う)。
- **pkmap は LSN 付きのバージョン化キー**: キーを `(pk, lsn)`(lsn 降順に並ぶ
  エンコーディング)、値を `doc-id` または削除トゥームストーンとして blind put する。
  参照は「`lsn ≤ 自ビューの可視 LSN 上限` を満たす最大の lsn」のエントリを採用する
  (上限超のエントリを無視するだけでは、blind put に上書きされた旧値を復元できない)。
  可視 LSN 上限より古い重複バージョンとトゥームストーンの物理 GC はセグメント確定/
  compaction 時に、生存ビューの最小上限を考慮して行う。既存 PK への upsert / delete 後も
  旧ビューから旧文書が見え続けることをテストで確認する。

### ディスクレイアウト(独自設計)

```
<collection>/
  LOCK                     # fd-lock(ライタのみ排他取得。リーダは取らない)
  MANIFEST-<gen>           # serde(JSON)。format_version, スキーマ, セグメント一覧,
                           #   次 doc-id, チェックポイント LSN, 各ファイルの世代番号
  kv/                      # RocksDB ×1(WAL 無効・atomic_flush): pkmap CF +
                           #   書き込み中セグメントの転置/FTS CF
  deletes-<gen>.bin        # RoaringTreemap スナップショット(世代付き)
  seg-<id>/
    wal-<n>.log            # 書き込み中セグメントの WAL(flush 毎にローテーション)
    forward-<n>.arrow      # Arrow IPC。flush 毎に不変チャンクを追加(追記や上書きはしない)
    scalar.idx             # 確定時に変換される不変の転置インデックス(自前形式)
    fts.idx                # 確定時に変換される不変の FTS ポスティング(自前形式)
    <field>.vidx           # ベクトルインデックス(自前バイナリ, magic+version+CRC32C)
```

- ファイル名の連番 `<n>` はすべてコミット済みマニフェスト由来(ディスク列挙から決めない)。
  マニフェストが参照しない孤児ファイルはリカバリ時に隔離/削除する。**ただし WAL は
  孤児規則の対象外**: WAL は命名規則 + ヘッダ/CRC 検証で発見し、チェックポイントより
  新しい世代を(封印済み→アクティブの順に)連続リプレイする。ローテーション直後・
  マニフェスト公開前のクラッシュでも、後続 WAL の承認済み書き込みを失わない。
- すべての永続アーティファクトは tmp 名で作成 → fsync → rename → **親ディレクトリ fsync**
  を経てからマニフェストに載る。
- 可変状態(pkmap、書き込み中セグメントの転置/FTS)はコレクション単位の単一 RocksDB に
  集約する(セグメント毎にインスタンスを持つとメモリ/スレッドが爆発するため)。セグメント
  確定時に該当キー範囲を不変ファイル形式へ変換し、ピン中のビューが無くなってから KV から
  削除する。
- **セグメントの寿命管理**: リーダは §0 の読み取りビューをピンする。compaction / destroy は
  参照が尽きるまで物理削除を遅延(POSIX では unlink 後も mmap は有効。Windows では削除
  失敗時のリトライ/遅延削除を実装し、テストする)。

## 2. マイルストーン

> **リリース区切り**: v0.1 = M0〜M5(基盤 + Flat/HNSW + スカラーフィルタ + 耐久性)。
> この時点で「永続化される・落ちても壊れない・フィルタ付き ANN 検索ができる」実用最小構成
> になる。v0.2 以降 = M6〜M12(量子化・FTS・ハイブリッド・疎・DDL・ディスクグラフ)。
> マイルストーンの実行順序は変更しない(M0〜M5 は既に連続した依存関係になっている)。

### M0 — ワークスペース基盤

- [x] Cargo workspace 雛形
- [x] `rust-toolchain.toml` で stable ツールチェインを固定し、components に rustfmt / clippy を
  指定。MSRV 方針を ADR に記録
- [x] lint は workspace ルートの `[workspace.lints]` に一元化し、
  `unsafe_op_in_unsafe_fn = "deny"` を設定。clippy の pedantic グループは選別して採用し、
  採否を記録。`rustfmt.toml` を置き、`-D warnings` は CI 側で付与
- [x] GitHub Actions の `ci.yml` を作成。PR と main への push で fmt チェック →
  clippy(`-D warnings`)→ test を Linux x86_64 / macOS aarch64 のマトリクスで実行し、
  `Swatinem/rust-cache` でビルドをキャッシュ
- [x] Linux の nightly ジョブで ASan / TSan のテストを実行。miri は FFI・mmap・intrinsics を
  含まない純ロジック codec(シリアライズ round-trip、WAL/マニフェストレコードのパース、
  varint / ビットパック)のみに限定して適用
- [x] `cargo-deny` ジョブでライセンス・脆弱性 advisory・重複依存をチェックし、上記の
  CI ジョブをすべて required checks に設定(required checks の有効化のみ未了:
  マージ後に AGENTS.md 記載の手順で管理者が設定する)
- [x] `AGENTS.md` を作成し、`CLAUDE.md` はそれを参照する形で作成。ビルド / テスト / lint の
  コマンド一覧、ワークスペース構成の要約、コーディング規約(unsafe は SIMD / mmap /
  グラフ公開プロトコルの 3 箇所に限定、依存クレートのエラーは自前 `Error` へ変換、公開 API に
  依存クレート型を出さない)、ADR の作成手順を記載。`todo.md` をロードマップの単一情報源とし、
  各マイルストーンの完了条件で進捗を判定する運用も定める
- [x] `target/` 等を指定した `.gitignore` と `.editorconfig` を作成
- [x] `vectordb-core`: `DataType` / `MetricType`(L2, InnerProduct, Cosine)/ `DocId` / 制限値
- [x] エラー設計: `thiserror` ベースの `Error` + `Result<T>`。カテゴリは
  InvalidArgument / NotFound / AlreadyExists / Corruption / Io / Unsupported / Internal。
  依存クレートのエラーはすべて自前バリアントに変換
- [x] `Schema` / `FieldSchema` ビルダー API と検証(名前規則、型と索引の整合)
- [x] `Doc` 型: typed setter/getter、null、検証(次元一致、有限値、疎ベクトルのソート・
  重複除去)
- [x] **機能マトリクス**を docs/ に作成: フィールド型 × インデックス型 × メトリクス × 量子化
  の対応表と、クエリ機能(radius、linear 強制、refine、group-by、ハイブリッド)の対応
  マイルストーンを一覧化。意図的な非対応はここに明記(§4 と同期)

**完了条件**: スキーマと Doc の検証がユニットテストで網羅され、`cargo clippy -- -D warnings` が
通る。CI の全ジョブがグリーンであり、`AGENTS.md` / `CLAUDE.md` に記載したコマンドが実際に
動作する。

### M1 — 距離カーネル(vectordb-simd)

- [ ] スカラー参照実装: f32/f16/i8 × {squared L2, inner product(負値で「小さいほど良い」に統一), cosine}
- [ ] cosine は「挿入時正規化 + IP」で実装(元ノルムは別途保持し復元可能に)
- [ ] ランタイム CPU 検出 + ディスパッチ。**カーネル毎の必要機能マトリクス**を定義:
  f32 → AVX2 / AVX-512 / NEON、f16 → x86 は F16C で f32 変換後に f32 演算(AVX-512FP16 は
  任意対応)、aarch64 は NEON+fp16 でネイティブ演算、i8 → AVX2 / AVX512-VNNI / NEON dot。
  f16 の利点は帯域/容量であり演算速度の向上を謳わない
- [ ] 各経路の強制実行テスト(dispatch を固定して全経路を CI で実行)+ 未対応 CPU での
  スカラーフォールバック
- [ ] 1対多バッチ距離 API(グラフ探索のホットパス)+ prefetch フック
- [ ] proptest: SIMD 実装とスカラー実装の許容誤差内一致(次元 1..=4096 サンプル)

**完了条件**: criterion ベンチが CI 記録として残り、`#[inline(never)]` + 自動ベクトル化を
抑止したスカラー基準実装に対する高速化係数が計測・記録されている(環境依存の固定倍率は
ゲートにしない)。

### M2 — Flat インデックス + 検索コントラクト(最小の動くもの)

- [ ] **`SearchRequest` を先に確定**(以後のマイルストーンはバリアント追加のみで拡張):
  - topk / 任意の radius(距離閾値。topk と併用時は「閾値内から topk 件」)/
    exact(linear 強制)フラグ / refine 倍率(exact 指定時は refine 無効、優先順位を文書化)
  - インデックス種別毎の typed パラメータ enum(ef, nprobe, beam width, …)
  - `Filter { allow: Option<RoaringTreemap>, predicate: Option<Arc<dyn Fn(DocId) -> bool + Send + Sync>>, selectivity_hint: Option<f32> }`
    - すべて **グローバル DocId** を受け取る。セグメント内ローカル id からの変換は
      各インデックス実装の責務(境界で変換)。allow / predicate / 削除 bitmap は
      **論理積(AND)** で合成
  - 結果コレクタ: セグメント毎にローカルコレクタを生成するファクトリ + 決定的マージ
    (通常 topk ヒープ / group-by コレクタを差し替え可能に)
  - `SearchRequest: Send + Sync` を static assert(セグメント並列実行の前提)
- [ ] **フィルタの意味論を確定**: フィルタで除外される doc はグラフ遍歴では通過可能・結果
  には不採用(「遍歴可・結果不可」)。この極性と契約を trait doc に明記
- [ ] `VectorIndex` トレイト: `insert / search(&SearchRequest) / flush / load / memory_usage`
- [ ] Flat(総当たり)インデックス: 連続バッファ + rayon 並列スキャン + topk ヒープ
- [ ] 永続化: magic + version + CRC32C 付き自前フォーマット、mmap ロード
- [ ] 正確性テスト: ランダムデータで線形スキャン結果と完全一致(Flat は厳密一致が正当)。
  **ローカル id が重複する 2 セグメント構成**で allow/predicate が正しく効くテスト

**完了条件**: 10 万 × 768 次元でインデックス作成→保存→ロード→検索の E2E テストが通り、
`SearchRequest` に radius / exact / collector / 合成規則が含まれている。

### M3 — ストレージ層(vectordb-store)

- [ ] **KV スパイクを最初に実施**: KV trait の要求能力(アトミックバッチ、順序付き
  prefix/range イテレーション、CF/名前空間、**スナップショット**、read-only オープン、
  flush/checkpoint、atomic_flush 相当、エラー写像)を先に定義し、rocksdb のビルド時間・
  サイズを計測。過大なら redb 等で同 trait を満たせるか判断して ADR に記録
- [ ] **耐久性ステートマシンを文書化してから実装**:
  - WAL レコード: 長さ + LSN(単調増加 op-id)+ 操作種別 + 旧/新 doc-id + ペイロード +
    CRC32C。フレーミングのバイト順序・フィールド配置は独自に設計し ADR に記録。
    WAL 追記はコレクション内で直列化(atomic doc-id 割当と同一クリティカルセクション)
  - **書き込みカット**: flush 開始時に write ロック下でカット LSN `L` を確定し、
    アクティブ WAL をローテーション(`wal-<n>` を封印し新 `wal-<n+1>` へ)。flush は
    `LSN ≤ L` のみを対象にし、削除するのは封印済み WAL のみ。flush 中も新 WAL への
    書き込みは継続できる
  - flush 手順(この順序を固定): (1) 封印 WAL fsync → (2) forward 不変チャンク →
    (3) ベクトル/転置/FTS の確定分 → (4) KV flush(全 CF atomic)→ (5) deletes-<gen+1> →
    (6) MANIFEST-<gen+1>(チェックポイント LSN = L)→ (7) 封印 WAL・旧世代ファイル削除。
    (2)(3)(5)(6) はすべて tmp → fsync → rename → dir fsync
  - **KV が先行する状態への防御は二段**: (i) log-ahead-of-data 規則(§0)を RocksDB の
    flush イベントフック(OnFlushBegin 相当)で強制し、「WAL に無い操作が KV に永続化
    される」ことを排除する。(ii) それでも KV はチェックポイントより新しくなりうる
    (WAL には在るが manifest 未公開)ため、リプレイ遷移表 + バージョン化 pkmap で吸収する。
    強制 memtable flush + WAL 末尾破棄を組み合わせたリカバリテストを必須とする
  - **フックのロック規約**: flush イベントフックはコレクションの write/WAL ミューテックスを
    **取得してはならない**(RocksDB の write stall とミューテックスの循環でデッドロック
    するため)。ライタは WAL 追記後・KV put 前に不変の `(required_lsn, WAL ファイル参照)`
    を atomic に公開し、フック/独立した sync コーディネータはそれだけを読んで fsync する。
    write stall + フック + ローテーション交錯の決定的テストを置く
  - マニフェストは KV のシーケンス番号(flush 時点)と可視 LSN 上限を記録する
  - リカバリ: 最大 gen のマニフェスト → WAL 発見(命名規則。孤児規則の対象外)→
    WAL 以外の孤児アーティファクトの隔離 → `LSN > checkpoint` の WAL レコードを封印済み
    →アクティブの順に遷移表に従い冪等リプレイ(各操作の Status を捕捉・集計)→
    next doc-id / next LSN を再計算。**リカバリを 2 回実行して結果が不変**であることをテスト
  - fsync 失敗はコレクションを poisoned(read-only)に遷移
  - **別プロセス read-only の KV アクセスモードを固定**: 同一 DB への `Open` と
    `OpenForReadOnly` の同時使用は避け、セカンダリオープン(catch-up 型)を採用。
    セカンダリは **`latest_sequence ≥ manifest.kv_sequence` まで catch-up してから**
    KV スナップショットを取得する(到達不能ならリトライ/失敗)。その上でマニフェストの
    可視 LSN 上限でバージョン解決する。遅延セカンダリと先行セカンダリの両方をテスト
- [ ] **WAL 破損の扱い**: 末尾の書きかけレコード(長さ不足・最終レコードの CRC 不一致)のみ
  切り詰めて許容。それ以外(中間レコードの CRC 不一致、不正長、未知操作種別)は
  `Corruption` として書き込み可能オープンを拒否(read-only + 明示リカバリ API は別途)
- [ ] **自動 flush トリガ**: WAL サイズ / 操作数 / 経過時間の閾値で自動 flush(設定可能)。
  WAL サイズに上限を設け、リプレイ時間を有界化
- [ ] forward store: 書き込み中はメモリ上の RecordBatch、flush 毎に `forward-<n>.arrow` の
  不変チャンクを追加。既存チャンクの上書き・追記はしない
- [ ] Arrow IPC 読み: 圧縮なし・64 バイト境界パディングで書き、`FileDecoder` +
  mmap `Buffer` によるゼロコピー経路を使用(既定の `FileReader` はコピーする)。
  検証はバッファアドレスの同一性とアロケーション計数で行う
- [ ] PK マップ(KV)と削除ストア(RoaringTreemap、世代付きスナップショット)
- [ ] **`FlushParticipant` 契約**: flush 手順(2)(3) に参加するコンポーネントの trait を定義し、
  フォールト注入フェイクでテスト。M5(scalar.idx)/ M7(fts.idx)は participant 登録後に
  M3 のクラッシュ行列を**再実行**する
- [ ] セグメント: 書き込み中(WAL + メモリ + KV)/ 確定済み(不変ファイル群)。doc-id 範囲は
  セグメント間で非重複・単調。読み取りビューは §0 の 4 要素エポック {manifest 世代,
  削除スナップショット, KV スナップショット, 可視 LSN 上限} を単一の `arc-swap` で
  アトミックに公開・ピンし、遅延削除を実装
- [ ] `Collection`: create / open / insert / upsert / update / delete /
  fetch(pks, `FetchOptions { projection, with_vectors }`) / flush / destroy
  (CRUD 意味論表どおり。セグメント跨ぎ・リプレイ後のテストを含む)
- [ ] fd-lock: **ライタのみ**排他ロック(ガードは Collection 生存中保持)。read-only
  オープンはロックなし + ビューのスナップショット分離。ライタ×ライタ排他 /
  リーダ並行動作 / クラッシュ後のロック解放 / destroy を**サブプロセスで**テスト

**完了条件**: (a) サブプロセス SIGKILL テストで flush 済みデータの完全残存と未 flush 分の
WAL 回復、(b) **注入可能な擬似 FS**(未 fsync データ破棄・部分書き込み・fsync/rename 失敗を
注入)で 7 ステップの全境界 + 「WAL ローテーション直後・マニフェスト公開前」境界 +
「強制 memtable flush + WAL 末尾破棄」ケースについて、回復後モデル(PK 一意性、可視文書
集合、削除メンバーシップ、doc-id/LSN 単調性)のアサートが通る、(c) リカバリ 2 回実行の
冪等性、(d) 中間レコード破損での writable open 拒否、(e) KV flush(step 4)とマニフェスト
公開(step 6)の間にサブプロセス read-only リーダを開くバリアテストで、旧ビューの一貫性が
保たれる — 新 doc-id が見えない**かつ**、既存 PK への upsert/delete 直後でも旧文書が
旧ビューから引き続き見える(NotFound にならない)ことの両方をアサートする。

### M4 — HNSW(ストリーミング挿入対応)

- [ ] グラフ構造: レベル生成(幾何分布)、上位レベルは貪欲降下、レベル 0 で ef 探索
- [ ] 近傍選択ヒューリスティック(候補の多様性チェック + 逆リンク刈り込み)
- [ ] パラメータ: `m`(既定 16, 5..=128)、`ef_construction`(既定 200, ..=2048)、
  検索時 `ef`(既定 100, topk との max を取る)
- [ ] **並行性プロトコル(表現を先に確定、ADR 化)**:
  - **追記**: 固定容量スロット + atomic 長さの release/acquire 公開。公開済み
    プレフィックス(`slots[0..len]`)は不変であることを保証し、リーダはロックなしで読む
  - **刈り込み/縮小/置換(公開済みスロットの変更)は in-place 禁止**: 新しい隣接リストを
    別領域に構築し、atomic ポインタ swap(`arc-swap` / `crossbeam-epoch`)で COW 公開。
    旧リストはピン中のリーダが尽きてから回収
  - エントリポイント・最大レベルの更新も atomic 公開
  - 挿入同士はノード単位ロックで直列化
- [ ] loom: 追記(スロット書込→長さ公開→読み)**と** COW 置換(swap→回収)の両方を
  モデル検査(必須)。ThreadSanitizer 下での並行挿入+検索ストレスを CI に追加
  (実測スモークは補助であり正しさの根拠にしない)
- [ ] visited 管理: 世代スタンプ付き byte-map(スレッドローカルプール)
- [ ] 削除対応: 検索時 filter で除外(「遍歴可・結果不可」)。グラフ修復はせず compaction で再構築
- [ ] 永続化: ノード配列 + 近傍リストを連続レイアウトで dump / mmap ロード
- [ ] リコールテスト: シード固定の混合ガウス(クラスタ化)生成器 + 総当たりの正解集合を
  リポジトリに定義し、recall@10 ≥ 0.95(ef=100, 100k, dim=128)。一様乱数は高次元で
  近傍が縮退するため使わない

**完了条件**: リコールテスト、loom モデル(追記 + COW 置換)、TSan ストレスがすべて CI で通る。

### M5 — スカラーフィルタ(vectordb-scalar)

- [ ] フィルタ式言語: SQL WHERE 風。`= != < <= > >= LIKE IN AND OR NOT`、配列の
  `contains_all / contains_any`、`IS NULL`、文字列 `prefix / suffix`
- [ ] パーサ: 自前 nom(WHERE 相当の有界文法に full-SQL パーサは過剰。sqlparser-rs 採用は
  比較検討の上 ADR に記録)
- [ ] 転置インデックス(KV): 順序保存エンコーディング(整数は符号ビット反転、
  浮動小数は符号に応じたビット反転)で範囲クエリを prefix scan に落とす。
  順序保存性の proptest を置く
- [ ] ポスティング: 小さければ配列、大きければ Roaring(閾値で自動切替)。KV 上の更新は
  blind put のみで表現できるキー設計にする(リプレイ冪等性の前提)
- [ ] 実行計画: (a) 転置で解ける述語 → bitmap、(b) 残りは forward データで逐次評価。
  選択率に応じて「事前 bitmap → ANN の filter」と「候補集合を直接ブルートフォース」を
  切替(既定閾値 5%、設定可能)
- [ ] `Collection::query` に filter 結合、`delete_by_filter` 実装
- [ ] セグメント確定時: 書き込み中の転置(KV)を不変 `scalar.idx` へ変換
  (`FlushParticipant` として登録し、M3 クラッシュ行列を再実行)

**完了条件**: フィルタ式のゴールデンテスト(パース→実行)一式。フィルタ付き検索は
Flat で線形評価と厳密一致、HNSW では「禁止 doc の混入ゼロ + フィルタ済み正解集合に
対する recall@10 ≥ 0.9」を選択率 {0.1%, 1%, 10%, 50%} で満たす。M3 クラッシュ行列の再実行。

### M6 — 量子化 + IVF

- [ ] F16 量子化(`half`): 保存・距離計算、リコール劣化テスト
- [ ] I8 / I4 量子化: アフィン量子化(スケール+バイアス)で、距離はメトリクスの展開式から
  導出した補正項付きで計算する(導出を ADR に記載)。粒度(レコード毎/全体)は
  リコールとメモリのベンチで決定。SIMD カーネル(AVX2 / VNNI / NEON)。I4 は偶数次元のみ
- [ ] 学習型回転(ランダム直交/学習直交変換)オプション: I8/I4 の前処理として適用可能に
  し、変換行列を量子化状態と共に永続化・再ロード。リコール改善をベンチで記録
- [ ] 量子化インデックス構成: 「量子化した本体 + 生ベクトルでの再ランク(refine)」を
  検索パイプラインとしてサポート(`SearchRequest.refine` 倍率)
- [ ] IVF: k-means(rayon 並列)。`n_list` 既定は `4·√N` の自動決定(手動指定可)、
  反復は改善が閾値未満になるまで(上限 20)。`nprobe` は `SearchRequest` で指定
- [ ] IVF はビルド型(一定件数を蓄積 → train → build。それまでは Flat フォールバック)
- [ ] (任意)MIPS→L2 変換メトリクス(ノルム拡張次元による最大内積検索)を Flat/HNSW で
  オプション対応。対応しない場合は §4 と機能マトリクスに明記

**完了条件**: 各量子化に recall/圧縮率のベンチレポート(シード固定データ、正解集合付き)。
I8+refine で recall@10 ≥ 0.93(M4 と同一データ・同一プロトコル)。回転の永続化 round-trip テスト。

### M7 — 全文検索(vectordb-text)

- [ ] トークナイザ抽象: 言語非依存の標準トークナイザ(Unicode 単語分割)/ 空白区切り /
  CJK 対応の辞書ベーストークナイザ、の 3 系を提供。正規化フィルタ(小文字化・
  ダイアクリティクス除去・言語別ステミング)をパイプラインとして合成可能にし、
  構成(辞書パス含む)はインデックスパラメータとして永続化
- [ ] インデックス(KV CF): term → ポスティング(Roaring)、位置リスト(delta-varint)、
  TF、文書長、コレクション統計。KV 更新は blind put のみ(蓄積構築は確定時)
- [ ] セグメント確定時に不変 `fts.idx` へ変換: ブロック分割 + delta + ビットパック +
  ブロック毎 max-score(Block-Max WAND 用)。ブロックサイズと端数ブロックの扱いは
  ビットパック実装の制約とベンチに基づき ADR で決定。`FlushParticipant` 登録 +
  M3 クラッシュ行列再実行
- [ ] クエリ言語(構造化): AND / OR / NOT(優先順位定義)、括弧、フレーズ、
  フィールドスコープ(`field:term`)、must/must_not(`+term` / `-term`)、
  boost(`term^2`)、エスケープ。+ 自然文モード(トークナイズ → OR / AND 結合)
- [ ] スコアリング: BM25(k1=1.2, b=0.75)+ WAND / Block-Max による topk 打ち切り
- [ ] フレーズ検索(位置リスト検証)
- [ ] スキーマ統合: String フィールドに FTS インデックスを付与、`query` から FTS 句を発行

**完了条件**: 既知コーパスでの BM25 スコアのゴールデンテスト、構造化クエリ(フィールド
スコープ・must/must_not・boost・フレーズ含む)の正確性テスト、WAND 有無で「決定的
タイブレーク(スコア降順 → doc-id 昇順)を定義した上で」結果集合が一致するテスト。
M3 クラッシュ行列の再実行。

### M8 — クエリエンジン統合とハイブリッド検索

- [ ] クエリプランナ: 単一クエリを {ベクトル検索, FTS, 転置のみ, 全走査} に振り分け、
  セグメント毎に並列実行(専用スレッドプール)→ コレクタの決定的マージ(スコア方向を統一)
- [ ] マルチクエリ(ハイブリッド): 複数サブクエリ(ベクトル×N + FTS)を実行し、
  **サブクエリ毎の取得数(per-query limit)を最終 topk と独立に指定可能**にした上で
  リランカーで融合。RRF(既定 k=60)/ 重み付き(スコア正規化はメトリクス毎の単調写像を
  定義し文書化)/ ユーザーコールバック。融合キーは PK
- [ ] group-by 検索: M2 のコレクタ hook を実装(グループフィールド、グループ数、
  グループ内 topk)。**通常 topk を取ってから群別するのではなく**、探索中にグループ
  充足数を追跡し、不足時は探索を延長する。null グループ・タイの扱いを定義
- [ ] radius(距離閾値)検索と exact(linear 強制)を planner まで貫通
- [ ] projection(射影)、with_vectors、with_ids(グローバル DocId の返却)オプション
- [ ] 検索結果: pk, score, フィールド値(+ オプションで doc_id)。スコアの意味
  (距離 or 類似度)を型で明示

**完了条件**: ハイブリッド検索の E2E テスト(ベクトル+FTS+フィルタの組合せ行列)、
RRF / 重み付き融合のゴールデンテスト、group-by の偏在データテスト(1 グループ支配的な
データで他グループが返ること)+ フィルタ・削除との組合せ。

### M9 — 疎ベクトル

- [ ] 疎ベクトル表現(u32 index + f32/f16 value、ソート済み)と dot product カーネル
- [ ] Flat 疎インデックス(全走査 + topk)
- [ ] 疎 HNSW(密 HNSW とグラフ構造を共有、距離関数と格納のみ差し替え)
- [ ] クエリ側の低値プルーニング(合計質量に対する budget で足切り)オプション
- [ ] メトリクスは inner product のみ許可(スキーマ検証)

**完了条件**: 疎検索の正確性テスト(線形スキャン一致)と、密+疎ハイブリッドの E2E テスト。

### M10 — DDL・最適化・運用系

- [ ] `create_index / drop_index`(既存データの再インデックス。ベクトルフィールドは
  インデックス未指定時 Flat として検索可能)
- [ ] `add_column`(バックフィル式 or null。non-nullable 追加は式必須)/ `drop_column` /
  `alter_column`(rename に加え、新しい FieldSchema の受け入れ: nullable 化・
  数値型の拡大変換をサポート。縮小変換は Unsupported)
- [ ] `optimize()`: 小セグメントのマージ、削除率が閾値(既定 25%、設定可能)を超えた
  セグメントの compaction(削除を filter しつつ再構築)、未ビルドのベクトルインデックス構築
- [ ] `stats()`: 文書数、セグメント数/内訳、インデックス完成度、メモリ使用量の推計
- [ ] グローバル設定: 検索/最適化スレッド数、ログ(`tracing`)、プランナ閾値
  (ブルートフォース切替率、転置→全走査切替率)、トークナイザ辞書ディレクトリ
- [ ] メモリ管理: 単一のハード上限ではなく、サブシステム毎の設定(KV ブロックキャッシュ、
  インデックスの mmap/ヒープ選択、書き込みバッファ上限)+ `stats()` での推計報告と
  して定義する(mmap 常駐分は OS 管理でありハード制御不能なため)
- [ ] `enable_mmap` はコレクション作成時に固定

**完了条件**: optimize 前後で、exact インデックスは検索結果が不変(削除分を除く)、
ANN インデックスは正解集合に対する recall@10 の劣化が ε=0.01 以内(同一プロトコル比較)。
DDL 途中クラッシュ→再オープンで一貫状態に戻るテスト(サブプロセス + フォールト注入)。

### M11 — グラフインデックス拡張(ストリーミング単層 + ディスク常駐)

- [ ] **ストリーミング単層グラフ**(メモリ常駐): robust prune(alpha 既定 1.2)、
  逆リンク維持、飽和オプション、オンライン挿入・検索・永続化。M4 の並行公開
  プロトコルと robust prune コンポーネントを共有
- [ ] **ディスク常駐グラフ**(大規模データ用):
  - ディスクレイアウト: 4KiB セクタ整列、ノードレコード = 生ベクトル + 近傍リスト。
    セクタ跨ぎ対応、entry point 近傍の BFS キャッシュ(**メモリ予算指定**)
  - PQ(積量子化): チャンク毎 256 セントロイド k-means、メモリ常駐 PQ コードで
    グラフ探索を誘導し、訪問ノードは生ベクトルで正確距離
  - ビーム検索 I/O: beam width 分のセクタをバッチ読み(まず `pread` 実装、
    io_uring は feature flag で後続)
  - ビルドはオフライン型(蓄積 → build → 差し替え)。ビルド中も旧インデックスで検索可
- [ ] ベンチプロトコルを実装前に ADR で固定: シード付きデータセット(1M 件)、
  warm/cold の別、RSS+mmap 常駐の計測方法、recall / p99 / IO 回数の**上限値**

**完了条件**: recall@10 ≥ 0.9(全 CI 環境)。RAM 制限下(cgroup でインデックスサイズの
1/4)の p99 / IO 上限ゲートは **Linux CI 限定**で実施し、macOS は recall ゲートのみ。

### M12 — 公開 API 仕上げ・ドキュメント・ベンチ

- [ ] API 最終レビュー: Rust API Guidelines 準拠(命名、`#[non_exhaustive]`、
  ビルダーパターン、`Send + Sync` 境界の明示)。依存クレート型の非露出を machine-check
  (public API の型走査)で確認
- [ ] rustdoc: 全公開型にサンプルコード、クレートトップに Quickstart、コミット意味論と
  並行性契約の明文化
- [ ] examples/: 基本 CRUD、ハイブリッド検索、フィルタ、大規模インデックス
- [ ] ベンチスイート: 挿入スループット、QPS(topk=10, 各インデックス)、リコール曲線
  (すべてシード固定・プロトコル文書化)
- [ ] fuzz: WAL / マニフェスト / 各不変インデックスファイルのパーサに cargo-fuzz
- [ ] (任意)pyo3 バインディング、C API

**完了条件**: `cargo doc` 警告ゼロ、examples が CI で実行され、README にベンチ結果と
計測プロトコルを掲載。

## 3. 横断的タスク

- [ ] **テスト戦略**: 各層に (1) ユニット、(2) 線形実装との等価性(exact は厳密一致・
  ANN は recall プロトコル)、(3) クラッシュ注入(サブプロセス SIGKILL + 擬似 FS
  フォールト注入 + リカバリ二重実行)、(4) proptest(シリアライズ round-trip、順序保存
  エンコーディング)、(5) 並行ストレス(TSan)+ loom(公開プリミティブ)
- [ ] **擬似 FS(フォールト注入レイヤ)**: 未 fsync データの破棄・部分書き込み・
  fsync/rename の失敗を注入できる VFS 抽象を M3 で整備(以後の全ストレージテストの基盤)
- [ ] **ADR** を `docs/adr/` に蓄積: KV 選定、パーサ選定、フォーマット設計(WAL フレー
  ミング・ポスティングブロックサイズ含む)、cosine 正規化、グラフ並行公開プロトコル、
  量子化補正式の導出、ベンチプロトコルなど
- [ ] **エラーパス検証**: すべての public API に不正入力テスト(次元不一致、型不一致、
  未知フィールド、read-only 違反、二重オープン、PK 重複/不在、非有限値)
- [ ] **プラットフォーム**: Linux x86_64 / macOS aarch64 を CI で常時、Windows は M10 以降
  (ファイル削除セマンティクスのテストを含む)

## 4. 非目標(明示)

- 分散・レプリケーション・サーバーモードは対象外(インプロセスのみ)
- トランザクション(複数文書のアトミック書き込み)は対象外。バッチは文書毎に成否を返す
- 書き込みの複数プロセス同時実行は対象外(単一ライタ)
- SQL 全文(SELECT 構文)は対象外。フィルタ式は WHERE 相当のみ
- バイナリベクトル・F64 ベクトルは v1 非対応(型 enum には将来枠として予約可)
- バックアップ/整合スナップショットの export は v1 非対応(将来: flush + マニフェスト
  世代のファイルコピーで実現する設計余地を M3 のレイアウトで確保)
- NFS 等のネットワークファイルシステム上での動作保証はしない(advisory lock の制約)

## 5. リスクと先行検証(スパイク)

| リスク | 先行検証 |
|---|---|
| グラフ並行公開プロトコルの正しさ | M4 冒頭で loom モデル(追記 + COW 置換)を先に書き、単スレッド版 → 並行版の順で導入 |
| rocksdb クレートのビルド時間/サイズ | **M3 の最初**に KV 能力契約(バッチ/範囲走査/CF/スナップショット/atomic flush)を定義した上で計測。redb 等への差し替え判断を ADR に記録 |
| KV のバックグラウンド flush と WAL の整合 | M3 で log-ahead-of-data フック(flush イベントで WAL fsync を先行)を先に実装し、「強制 memtable flush + WAL 末尾破棄」を擬似 FS で再現してリプレイ遷移表 + LSN 付き値で回復できることをテスト |
| Arrow IPC mmap ゼロコピー | M3 で `FileDecoder` 経路のバッファアドレス検証(ページフォールトは性能指標のみ) |
| トークナイザ/ステマーの品質 | M7 冒頭でトークン化ゴールデンテストを先に書く |
| ディスクグラフの I/O 効率 | M11 前に pread バッチのプロトタイプで sector/query を計測 |
